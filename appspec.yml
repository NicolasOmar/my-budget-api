version: 0.0
os: linux
files:
  - source: /
    destination: /var/nodeapp/
hooks:
  AfterInstall:
    - location: scripts/npm-install.sh
      timeout: 30
      runas: root
  ApplicationStart:
    - location: scripts/start-server.sh
      timeout: 30
      runas: root